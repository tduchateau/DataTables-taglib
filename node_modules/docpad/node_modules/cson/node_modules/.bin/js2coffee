#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../js2coffee/bin/js2coffee" "$@"
  ret=$?
else 
  coffee  "$basedir/../js2coffee/bin/js2coffee" "$@"
  ret=$?
fi
exit $ret
